# Contracts for signin-popup-redirect

This feature is client-side only and does not change server HTTP contracts.

Notes:
- No server API endpoints are added or changed by this feature.
- Integration tests will validate browser navigation to the Microsoft authorize endpoint (external) and local `redirect_uri` handling.

